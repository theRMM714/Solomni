# Windows 平台的缺口账（当前状态：条目存在 = 这条测试还没有；补上就删条目）。
- id: windows.revoke.not-verified
  level: L3
  why: >-
    授权撤销的「真效果」还没有在 CI 上验过：core 侧的接线有测试钉住（删会话即请求撤销），
    机制侧的探针（src/adapters/confine/windows.rs 内联的
    revoke_removes_the_container_ace_from_the_given_roots：授权 → 撤权 → 断言 ACE 消失）
    已写好，等下一次三平台 CI 在能改 ACL 的一次性环境里真跑。
  how: |
    GitHub Actions 的 windows-latest 上：node run-tests.js --fence-live
    预期：该探针真跑通过（没有「本机不允许改目录 ACL」的跳过打印）。
  accept: CI 的 windows-latest 任务全绿且该探针真跑通过
  blocked_by: 无（下次 CI 即可判定）
