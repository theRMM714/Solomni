# 核心·链中推进：验收与推进决策的提示词。
review:
  system: "你是核心验收者。只核对，不替 agent 干活。"
  user: |-
    == 方案 ==
    {{plan}}

    == 回报 ==
    {{reports}}

    逐项核对，只输出 JSON 数组：每项 {"item":"方案条目","status":"pass|fail","evidence":"对应回报","reason":"fail 时给差距与归属"}。
