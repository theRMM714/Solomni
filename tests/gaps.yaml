# 全局测试与质量缺口（当前状态：条目存在 = 尚未完成；补上后删除条目）
#
# 这里只放**长期目标**：已经能机器判定的门禁不入账。平台与跨平台层的缺口记在
# tests/cross-platform/gaps.yaml 与 tests/<平台>/gaps.yaml（它们决定 TEST-REPORT-ACCEPTED）。

- id: quality.hard-gate-full
  scope: global
  level: T0
  why: |
    T0 现在分两级：编译与结构审查是硬失败；格式、clippy、编译告警、依赖重复走
    tests/quality-baseline.yaml 的存量基线（超出即失败，降到基线以下要求同步下调）。
    基线是过渡手段，不是终点——长期目标是存量清零后把这几项也升级成零容忍硬失败。
  how: |
    按 tests/quality-baseline.yaml 的清单逐项收敛：
    1) 决定是否接受 cargo fmt 的全仓格式化（会展开现在的紧凑写法）或加 rustfmt.toml 贴近现有风格，然后清空 fmt_deviating_files；
    2) 逐条修掉 clippy 存量：机械项直接修（unnecessary_cast / unnecessary_map_or / unnecessary_sort_by /
       collapsible_match / single_match / unnecessary_mut_passed / cloned_ref_to_slice_refs / items_after_test_module），
       设计取舍项（too_many_arguments / large_enum_variant）要么改签名，要么加**带理由的**窄 allow；
    3) check_warnings 归零；
    4) 为 duplicates 里的每个重复给出处理或保留理由（syn v2/v3 属 proc-macro 传递依赖，通常只能记录事实）。
    每收敛一项就重新生成基线，基线随之变小。
  accept: |
    tests/quality-baseline.yaml 里 fmt_deviating_files、clippy、check_warnings 全部为空/零；
    run-tests.js 的 T0 四项不再需要基线比对，直接以退出码与零容忍判定；
    删除本条目与 tests/quality-baseline.yaml。
  blocked_by: 需要先决定全仓格式化与「带理由 allow」这两项取舍

- id: tools.native-tool-calls
  scope: global
  level: T1
  why: |
    工具调用目前只有一条通道：模型在自由文本里**手写 JSON 信封**（type/module/name/args）。
    主流实现走供应商原生工具调用（参数由结构化槽位给出，模型不必手抄 JSON），
    本项目却把转义与括号配对的正确性押在模型身上——真实会话里已经出现
    "content 里一个裸换行导致整段 JSON 非法、连续三次写入失败"。
    两侧的补救已落地（信封不合法的四类判定 + 对应修法回执、read 行区间、edit、JSON 修复端口），
    但通道本身仍未做。
  how: |
    阶段 0（已落地）：models.yaml 的 tools: envelope|native（缺省 envelope），非法值拒收、编辑保留原值；
      登记处契约写明"两种形态互斥"（一个通道走 native 就只认原生调用）。
    阶段 1（已落地）：Chat 端口带 CompleteOpts{tools} 与 Completion.calls；HttpChat 两条路径都解析
      tool_calls（含流式按 index 拼分片）；探测端口 + 隐藏模式 --probe-tools（不带声明的先证明通道通不通，
      带声明的那条才在测支持；支持/明确不支持/无法判定三种结论都如实，只把确凿结论写回登记处）。
      真实中继实测：4sapi.cn/v1 对 glm-5.3-flash 支持原生调用（finish_reason=tool_calls）。
    阶段 2（未做，引擎侧，本轮因上下文预算停下，未动任何代码）：
      ① Msg 支持 assistant(tool_calls) 与 tool(tool_call_id)（适配器按 OpenAI 形状序列化）；
      ② 按通道形态决定这次请求带不带工具声明、回复按哪套协议解析；
      ③ 一次回复 N 个调用 → N 条工具行、结果按 id 回填（先串行；并发是阶段 4）；
      ④ ToolCallView 记下"这一行是什么形态"（native 记 id/name/args），重建历史按它拼消息，保证回放一致；
      ⑤ 系统提示按形态注入"直接调用工具"或"写信封"（两套文案互斥，不能同时教），
         形态变了就地刷新系统提示并给用户一句通知，没变什么都不做；
      ⑥ native 模式下正文里出现信封 → 不执行，但记一条失败工具行说清"本通道用原生调用"；
         声明 native 而供应商报错 → 如实报错，不静默降级成信封。
    阶段 3：文档（MODULE_SPEC/ARCHITECTURE/TESTING）+ 探测的 GUI 按钮（目前只有 --probe-tools）。
    阶段 4：工具声明加 parallel + 按声明并发（只读并发、写类独占），结果按原始顺序回填。
    另：patch 的 FREEFORM 通道已落地（阶段 1 之外的那半件事已完成）。
  accept: |
    供应商支持原生工具调用时，参数从结构化槽位直达工具，模型不再手写 JSON；
    端口矩阵新增一行（原生 tool_calls × 各供应商）且 cargo test 与 node run-tests.js 全绿。
  blocked_by: 未开始（本轮只做前两层）

- id: tools.multi-call-and-concurrency
  scope: global
  level: T1
  why: |
    一次问询里模型只能发**一个**工具调用：多个互不依赖的读取要串成多轮往返（每轮都是一次完整模型调用）。
    主流做法是一次回复里给出多个调用（如 Codex 的 multi_tool_use.parallel 包装工具），
    并按工具声明决定能否并发执行。本项目所有工具一律串行，read 这类只读工具也不并行。
  how: |
    1) 信封支持 {"type":"tool","calls":[{module?,name,args}, ...]}（与单数形态互斥；GREEN FIELD 不保留双形态兼容分支）；
    2) 每个调用各记一条工具行（前端视图不变），MAX_TOOL_CALLS 按**调用数**计；
    3) 回档/截断以"轮"为原子单位（一轮 = 一次模型回复 + 它触发的全部调用行）；
    4) 工具声明加"可并发"标记：只读类并发调度、写类独占、未声明默认串行；
       结果一律按原始顺序回填（防乱序进上下文）。
  accept: |
    一次回复里的多个调用都执行且各成一条工具行；声明可并发的工具真的并发（用记录型替身断言时序）；
    cargo test 与 node run-tests.js 全绿。
  blocked_by: 未开始（本轮只做前两层 + 信封修复端口）

- id: tests.layout-migration
  scope: global
  level: T2
  why: |
    端口契约测试现落在 src/contract_tests/（ports / fakes / adapters），替身仍在 src/tests.rs，
    两者同属 T2 却被拆在两层；长期应合成 src/tests/ 目录，让 doubles 与契约测试同处一层。
  how: |
    1) 建 src/tests/mod.rs，把 src/tests.rs 的 T1 用例移入；
    2) 把 InMemory*/Fake*/Recording*/ScriptGateway/TestPrompts 等替身集中到 src/tests/doubles.rs；
    3) 把 src/contract_tests/ 下的 ports / fakes / adapters / api / intent / routes 平移到 src/tests/ 下同名文件；
    4) 更新 src/main.rs 的 mod 声明与 TESTING.md §十一 与 §四 的路径引用。
    纯搬运，不改断言语义；每步保持 cargo test 全绿。
  accept: |
    src/contract_tests.rs 与 src/tests.rs 都不存在；src/tests/ 下 doubles/ports/fakes/adapters 各一文件；
    cargo test 与 node run-tests.js 全绿；TESTING.md 不再指向旧路径。
  blocked_by: 缺少实现
