# 模块契约（MODULE_SPEC）

> 模块与核心之间的全部约定。除此之外，双方互不依赖。
> 理念见 [PHILOSOPHY.md](PHILOSOPHY.md)，产品流程见 [PRODUCT.md](PRODUCT.md)。

## 设计立场

**自由是默认，锚点只有机器不得不判定的几处。**

- 讨论里怎么说话、任务怎么拆、中间产物用什么格式——完全自由，由 AI 自决
- 固定的结构只有机器不得不判定的几处，都只是文本；没有 SDK，没有基类，没有编译期依赖：
  ① 打包格式（module.yaml），② 发言信封，③ 回报与验收清单，④ 供应商引用（见「供应商与密钥」）

## 一、打包格式（module.yaml）

一个文件夹 = 一个模块。模块被识别的唯一条件：文件夹内有合法的 `module.yaml`。

```yaml
id: research            # 全局唯一，= 文件夹名
brief: >-               # 能力简述，两三句。给核心选人看，给菜单上的用户看。
  调研、对比与选型。只说明能干什么，不列具体技能。
system: >-              # 职责提示词。管辖边界 + 本模块工具说明。交付任务时注入模块 AI。
  你负责调研与对比……不碰知识库写入……
tools:                  # 工具表：工具名 → 启动命令。名字随 system 进入模块提示词；
  read_txt: python tools/read_txt.py          # 命令由核心按表放行并拉起（见「工具执行」）
  search_txt: python tools/search_txt.py
model:                  # 可选：默认供应商
  # prefer: high       # 预留字段（多模型供应商下按用途选模型），尚未接入
  # provider: deepseek  # 可选：默认供应商 id；运行期选择记录在 model.config.yaml
```

规则：

- `id` 与文件夹名一致且全局唯一；不一致 = 非法，核心拒收并说明原因（校验，不是挑选）
- `brief` 只说"能干什么"，不承诺具体技能；它是选人的唯一依据
- `system` 写管辖边界与工具用法；不要在 system 里复述讨论协议——协议由核心在集结时注入
- `userdata/` 是模块私有工作区：git 忽略、数据不出工作区；**密钥永不落在模块**——供应商与密钥归产品登记处（见「供应商与密钥」）
- 模块损坏或 yaml 非法 → 不进清单，菜单少一项，永不崩溃

## 二、发言信封（讨论/协作模式）

模块的每次回复是一个最小信封。**信封是唯一的机器锚**，信封里的 text 完全自由。

```json
{ "type": "say",   "text": "……自由发言……" }
{ "type": "ask",   "text": "向用户提出的问题" }
{ "type": "leave", "text": "（可选）退场留言" }
{ "type": "agree", "text": "（可选）同意意见" }
{ "type": "tool",  "name": "工具名", "args": { } }
```

| 动词 | 语义 | 核心的机械反应 |
|---|---|---|
| `say` | 发言 | 并入上下文，转给后续模块 |
| `ask` | 请教用户 | 核心中止轮转，把问题呈给用户；回答并入上下文 |
| `leave` | 退场 | 此后不再向该模块转达任何消息 |
| `agree` | 同意 | 记一票；在组模块全部 agree → 讨论终止 |
| `tool` | 申请用工具 | 核心查本模块工具表放行 → 拉起进程 → 结果回注（见「工具执行」） |

状态机规则：

- 讨论终止 = **留下的**模块全部 agree，或触及轮次上限（超限交用户裁决）
- leave 与 agree 均不可逆：退了不再被询问，同意了不再改口（要改口就是新一轮讨论）
- 聊天约定（发言顺序、礼貌、格式）由核心随首轮提示词注入，**可以自由演化**——
  约定本身是文本，不是信封的一部分

## 三、回报与验收（执行边界）

**任务回报**——模块每次交付执行结果必须给出，三问三答：

```json
{
  "summary": "做了什么",
  "changes": "动了什么（文件/数据/外部影响）",
  "open":    "遗留什么问题；没有则空"
}
```

**验收清单**——核心对照讨论形成的方案逐项核对，输出：

```json
[
  { "item": "方案条目", "status": "pass",   "evidence": "对应哪个模块的哪份回报" },
  { "item": "方案条目", "status": "fail",   "reason": "差距与归属模块" }
]
```

- 有 fail → 核心定位归属 → 返工：整体问题回讨论，定向问题回该模块
- 返工次数有上限，超限交用户裁决
- 全部 pass → 交付用户

## 四、工具执行

声明了 `tools` 的模块在**执行阶段与直连会话**里可以用工具；讨论阶段不接工具（讨论只出主意，干活才动手）。

- **调用**：模块 AI 发 `tool` 信封；参数是 JSON 对象，经 stdin 送入工具进程（不进命令行，杜绝注入）
- **放行**：核心只执行本模块 `tools` 表里声明过的工具；表外一律拒绝并把可用清单发回
- **运行**：工具进程的工作目录 = 模块根目录（`userdata/` 等私有区在其下）；命令语言不限，解释器须在运行机上可用
- **回注**：stdout/stderr（截断后）作为 `[工具结果]` 消息发回模块 AI，继续对话直到最终答复
- **上限**：单次问询工具调用有上限，超限强制收尾（上限必生效）
- **如实呈现**：每次调用以一行轨迹呈现给用户（成功/失败/未声明，一目了然）
- **边界如实**：工具进程权限等同运行产品的用户；权限分级与沙箱是后置工作，模块作者应把工具写成只碰自己工作区的样子

## 五、测试要求

- 每个模块可进行单独 mock 测试：给一段提示词，不依赖核心、不依赖真实模型
- 工具必须可以脱离讨论独立运行：参数走 stdin JSON、结果走 stdout，核心外可单独验证

## 六、供应商与密钥

**密钥归产品，不归模块。** 模块与工具只持引用（供应商 id），永不接触通道本身：

- 产品维护一份**供应商登记处**（核心私有区，git 忽略 + 0600）：每条记录 = id、端点、密钥、可用模型；用户经产品命令管理（添加 / 换钥 / 删除 / 设默认）
- **LLM 通道**：模块的 `model.config.yaml`（运行期选择记录，核心只读，git 忽略）只记录当前选用哪个供应商 id。优先级：模块当前选择 > `module.yaml` 默认 > 产品全局默认；所选不存在时回落默认并如实告知用户。写入路径待接入（见 [PRODUCT.md](PRODUCT.md) 后置工作）
- **工具密钥（待接入）**：工具配置同样只写供应商 id，由核心拉起工具进程时解析注入——执行器已接入最小版，密钥注入仍待实现
- **安全红线**：密钥只存在于登记处与核心的出站调用里——永不进入提示词、转录、日志、模块工作区
- **选择权**：模块用哪个供应商只由用户决定；模块 AI 请求换用时只能见到 id 清单，且须经用户确认

## 七、制作一个模块

1. 建文件夹，写 `module.yaml`（id、brief、system、tools）
2. 把工具放进文件夹，确认可独立运行
3. 单独试运行：只给 system + 一句用户需求，看它是否守边界、会用手头的工具
4. 放入 `modules/`——完成。清单自动出现它，菜单自动展示它的 brief

没有注册、没有继承、没有编译依赖。**一个文件夹就是全部。**
