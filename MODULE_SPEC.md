# 模块契约（MODULE_SPEC）

> 模块与核心之间的全部约定。除此之外，双方互不依赖。
> **本文只讲模块作者要遵守的契约**；代码分层、端口、日志、提示词册这些开发规则见 [ARCHITECTURE.md](ARCHITECTURE.md)，
> 登记处（供应商 / 模型 / agent / 设置）的字段见 [REGISTRY_SPEC.md](REGISTRY_SPEC.md)，
> 运行包契约见 [RUNTIME_SPEC.md](RUNTIME_SPEC.md)，理念见 [PHILOSOPHY.md](PHILOSOPHY.md)，产品流程见 [PRODUCT.md](PRODUCT.md)，
> 测试层级与替身规范见 [TESTING.md](TESTING.md)。

## 设计立场

**自由是默认，锚点只有机器不得不判定的几处。**

- 讨论里怎么说话、任务怎么拆、中间产物用什么格式——完全自由，由 AI 自决
- 固定的结构只有机器不得不判定的几处，都只是文本；没有 SDK，没有基类，没有编译期依赖：
  ① 打包格式（module.yaml），② 发言信封，③ 回报与验收清单，④ 供应商引用（见「供应商与密钥」）

## 一、打包格式（module.yaml）

一个文件夹 = 一个模块。模块被识别的唯一条件：文件夹内有合法的 `module.yaml`。

```yaml
id: research            # 全局唯一，= 文件夹名
brief: >-               # 能力简述，两三句。给核心选人看，给菜单上的用户看。
  调研、对比与选型。只说明能干什么，不列具体技能。
system: >-              # 职责提示词（能力说明）：管辖边界 + 本模块工具用法。被装进 agent 时与其它模块合成一份。
  你负责调研与对比……不碰知识库写入……
runtimes:               # 【可选】运行能力：本模块的工具需要哪些运行包（见 RUNTIME_SPEC.md）
  - python              # 只声明能力名，不写版本——版本由用户在会话的执行档位里定
tools:                  # 【可选】外部工具表：工具名 → 启动命令。名字随 system 进入模块提示词
  read_txt: python tools/read_txt.py          # 命令由核心按表放行并拉起（见「工具执行」）
  search_txt: python tools/search_txt.py      # read / write / search 是核心内置的保留名，模块不得占用
```

规则：

- `id` 与文件夹名一致且全局唯一；不一致 = 非法，核心拒收并说明原因（校验，不是挑选）
- `brief` 只说"能干什么"，不承诺具体技能；它是选人的唯一依据
- `system` 写管辖边界与工具用法；不要在 system 里复述讨论协议——协议由核心在集结时注入
- `runtimes` 只声明能力名（`python` / `node` / `bash` / `cc`……），格式不合法或重复声明 = 模块被拒收并说明原因；
  **声明不构成保证**：`tools` 里的命令仍是模块作者的自由文本，能力没装载 / 命令跑不起来都如实报错，不静默兜底
- `tools` 占用内置保留名（`read` / `write` / `search`）= 模块被拒收并说明原因（这三个名字归核心，见「内置文件工具与路径模型」）
- `userdata/` 是模块私有的**跨任务**状态区（缓存/记忆/配置）：git 忽略、数据不出工作区；
  一次工作的投喂与成品一律写进本次工作的共享区 / 你 agent 的沙箱（提示词里给出的真实根目录），不要塞进模块自己的 userdata；
  **密钥永不落在模块**——供应商与密钥归产品登记处（见「供应商与密钥」）
- 模块损坏或 yaml 非法 → 不进清单，菜单少一项，永不崩溃

## 二、发言信封（讨论/协作模式）

**模块不发言，agent 发言。** 模块只交两样东西：

- `system`：这份能力的职责提示词（管辖边界 + 本模块工具用法）
- `tools`：本模块声明的外部工具表

模块被装进哪个 agent，就以那个 agent 的名义说话。一个 agent 可以装多个模块，它的职责提示词 = 这些模块 `system` 的合成。
工具的身份是 **(模块 id, 工具名)** 这一对：**不同模块可以声明同名工具，互不冲突**——调用时在信封里写明 `module` 就各归各模块（模块内名字唯一由 `tools` 是映射表天然保证）。

agent 的每次回复是一个最小信封。**信封是唯一的机器锚**，信封里的 text 完全自由。

```json
{ "type": "say",   "text": "……自由发言……" }
{ "type": "ask",   "text": "向用户提出的问题" }
{ "type": "leave", "text": "（可选）退场留言" }
{ "type": "agree", "text": "（可选）同意意见" }
{ "type": "tool",  "module": "模块id", "name": "工具名", "args": { } }
```

- `type` 必填；`text` 字段可省略，省略时按空串处理（`leave` / `agree` 常只表态不留言）。
- 信封解析失败不猜测：按发言原文收录并标记降级（转录即内容）。
- 「看起来想发 `tool` 信封但 JSON 非法」是独立信号：**既不执行工具，也不把 JSON 当正文渲染**，
  核心记一条失败的工具行把"信封不合法"回注给模型，让它下一轮自己改。

| 动词 | 语义 | 核心的机械反应 |
|---|---|---|
| `say` | 发言 | 并入上下文，转给后续 agent |
| `ask` | 请教用户 | 核心中止轮转，把问题呈给用户；回答并入上下文 |
| `leave` | 退场 | 此后不再向该 agent 转达任何消息 |
| `agree` | 同意 | 记本轮一票；留下的 agent 本轮全部 agree → 讨论收敛 |
| `tool` | 申请用工具 | 核心按 (模块, 工具名) 查表放行 → 拉起进程 → 结果回注（见「工具执行」） |

状态机规则：

- 讨论终止 = **留下的** agent 本轮全部 agree，或触及轮次上限（超限交用户裁决）
- `leave` 不可逆：退了不再被询问
- `agree` 是**本轮**表态，不是终身承诺：进入新一轮即重置，需重新表态；
  用户也可以单独撤回某个 agent 的同意（`[用户:撤回]`），核心按剩余转录重新判定收敛
- 聊天约定（发言顺序、礼貌、格式）由核心随首轮提示词注入，**可以自由演化**——
  约定本身是文本，不是信封的一部分

## 三、回报与验收（执行边界）

**任务回报**——agent 每次交付执行结果必须给出，三问三答：

```json
{
  "summary": "做了什么",
  "changes": "动了什么（文件/数据/外部影响）",
  "open":    "遗留什么问题；没有则空"
}
```

**验收清单**——核心对照讨论形成的方案逐项核对，输出：

```json
[
  { "item": "方案条目", "status": "pass",   "evidence": "对应哪个 agent 的哪份回报" },
  { "item": "方案条目", "status": "fail",   "reason": "差距与归属 agent" }
]
```

- 有 fail → 核心定位归属 → 返工：整体问题回讨论，定向问题回该 agent
- 返工次数有上限，超限交用户裁决
- 全部 pass → 交付用户

## 四、工具执行

工具属于模块，使用权跟着装了它的 agent：**执行阶段与单 agent 的对话**里都能用工具；讨论阶段不接工具（讨论只出主意，干活才动手）。

- **调用**：模块 AI 发 `tool` 信封；参数是 JSON 对象，经 stdin 送入工具进程（不进命令行，杜绝注入）
- **编码**：stdin 与 stdout **统一 UTF-8**（核心为子进程强制 UTF-8；Windows 上 Python 默认按系统代码页解 stdin，
  不强制就会把中文参数解成代理转义字符、工具写盘即失败）
- **放行**：核心只执行本模块 `tools` 表里声明过的工具；表外一律拒绝并把可用清单发回
- **所属模块**：`module` 指明用哪个模块声明的工具；**省略时**只有该 agent 恰好一个模块才替它定，否则如实报错并列出可用的 `模块.工具`（不猜）。内置 `read`/`write`/`search` 属核心，不写 `module`
- **运行**：工具进程的工作目录 = **它所属模块**的根目录（`userdata/` 等私有区在其下）；命令语言不限，解释器须在运行机上可用
- **运行能力**：模块在 `runtimes` 里声明工具需要的运行包能力；执行环境随会话的**执行档位**而定——本机档在宿主上跑（宿主自备解释器），
  虚拟机档按声明装载运行包（见 [RUNTIME_SPEC.md](RUNTIME_SPEC.md)）。
  该能力不在包库里 = **该模块的工具不执行**，回执如实说明缺哪个能力（讨论与其它模块照常，不崩溃）；
  同一能力多版本而用户没定版、定版指定的版本不在库里、两个包写进同一处系统路径 = 虚拟机档「开始」那一刻如实拒绝（选型问题由用户解决）
- **回注**：stdout/stderr（截断后）作为 `[工具结果]` 消息发回模块 AI，继续对话直到最终答复
- **上限**：单次问询工具调用有上限，超限强制收尾（上限必生效）
- **如实呈现**：每次调用在转录里自成**一条工具行**（前端渲染成工具卡片：模块.工具名 + 成败，参数与结果折叠可展开）；未声明/越界也照此如实呈现
- **边界如实**：工具进程统一从**守门进程**里跑——环境白名单（不继承父进程环境，HOME/TEMP 落在该 agent 的私有沙箱）、
  超时连根杀掉整棵进程树、可达范围按平台围栏强制（Linux Landlock / macOS seatbelt / Windows AppContainer）。
  能力等级在启动时**如实报告**（装不上就说装不上）；模块作者仍应把工具写成只碰自己工作区的样子

### 内置文件工具与路径模型（无需模块声明）

核心自带三个工具，**名字保留**，模块的 `tools` 表不得占用（占用 = 模块被拒收）：

- `read`：读文本（严格 UTF-8），参数 `{"path":"…"}`
- `write`：写文本（一律 UTF-8，覆盖同名文件），参数 `{"path":"…","content":"…"}`
- `search`：关键词搜索，带行号的命中行 + 总数，参数 `{"path":"…","keyword":"…","ignore_case":true}`

**路径模型：AI 只看到真实绝对路径。** 仓库里（代码、提示词册、module.yaml、本文档）一律只用**占位符**，
运行时由核心取到下列真实目录再替换进提示词——这样仓库永远不含机器路径，而内置工具与模块自带的外部工具**用同一套路径语言**：

| 占位符（仓库里） | 运行时替换为 | 可达范围 |
| --- | --- | --- |
| `{{work_root}}` | 本次工作的共享区 `session/<工作名>/work/` 的真实绝对路径（用户投喂与成品） | 本工作内的 agent |
| `{{sandbox_root}}` | 该 agent 私有沙箱 `session/<工作名>/<agent实例名>/` 的真实绝对路径 | 只有它自己 |
| `{{module_roots}}` | 该 agent 各能力模块目录的真实绝对路径（一行一个） | 只有该模块所属的 agent；写进去会在回执里如实提示 |

- **越界即拒绝**：工具收到的路径必须是**列出的真实根目录之下的绝对路径**；相对路径、`..` 跳出、以及不在任何根之内的路径一律拒绝，并把允许的根目录列回去（如实报错，不纠正）。
- **编码**：读严格按 UTF-8，遇到非法字节按替换字符呈现并如实标注——**本程序不猜编码**；写一律 UTF-8。
  非 UTF-8 的文件怎么办由 AI 自己判断，不由核心代劳
- **用户也能引用**：用户在输入框里打 `@` 挑文件（语法 `@work:相对路径` / `@sandbox:<agent>/相对路径`，**给人用**），
  核心把它替换成**真实绝对路径之后**才进入转录与上下文——所以转录里写的与 agent 拿到的是同一个串。
  引用别的 agent 私有沙箱里的文件时，核心如实说明无权读取（且**不泄漏**别人的真实路径），不偷偷复制到共享区。
- **隔离级别如实**：内置工具的越界拒绝是**工具层**的（拼路径前逐段校验 + 只认本 agent 的根）；
  外部工具进程另有**进程级围栏**（守门进程 → 环境白名单 / 进程树围栏 / 按平台的文件系统围栏），
  实际强制到哪一步由启动报告如实给出——没有接入的就是没有接入

## 五、模块测试交付

模块交付时必须能说明并验证自己的行为边界。测试层级（T0-T5）、替身语义、隔离与清理、报告状态与缺口账格式
由 [TESTING.md](TESTING.md) 统一规定；本节只列**模块作者要交付什么**。

### 5.1 最小可测性

- 职责边界必须能在不依赖真实模型、真实供应商、真实密钥的条件下验证。
- 只提供 `system` 的模块，至少用一组可重复的输入场景检查职责边界、禁止事项与输出要求；这类检查不能伪装成工具进程测试。
- 声明 `tools` 的模块，工具必须能脱离核心独立运行：参数从 stdin 收 JSON、结果从 stdout 输出、失败经非零退出码或约定好的错误结果暴露。
- 测试只用模块契约允许的输入与路径模型，不写机器绝对路径、不依赖个人目录或真实凭据；测试之间相互隔离、不依赖执行顺序。

### 5.2 三类场景

每个能力先列适用场景，再按下表逐项给出结果或缺口：

```text
场景   | 输入/条件 | 预期结果 | 观察方式 | 当前状态
正常   | …         | …        | …        | pass / gap / env-skip
错误   | …         | …        | …        | pass / gap / env-skip
边界   | …         | …        | …        | pass / gap / env-skip / 不适用
```

- **正常**：最小有效输入、典型输入、一次完整成功流程；不用"没崩溃""返回非空"代替对行为的断言。
- **错误**：缺必填参数、格式错误、文件不存在、编码错误、依赖失败、超时、子进程非零退出、输出不可解析等；
  必须验证错误能被识别，且不会被静默改写成成功。
- **边界**：空输入、最小/最大长度、重复调用、中文与非 ASCII、换行差异、特殊字符、超大输出、并发或取消等。

`gap`、`env-skip` 与"不适用"都必须说明原因；不能用"以后补""本机没问题"这类不可判定的描述代替。

### 5.3 工具协议契约

对 `module.yaml` 里声明的每个工具，至少验证：

- stdin JSON 能被独立启动的工具正确解析；
- stdout 只输出约定的结果，不把调试信息混入协议；
- stderr、退出码与截断行为不会掩盖失败；
- 核心调用与独立启动使用同一套协议语义；
- 工具不存在、解释器不可用或运行能力缺失时，失败能如实暴露。

### 5.4 资源清理与跨平台假设

- 测试数据写入专用隔离根，不碰真实 `.home/`、真实 `session/`、模块 `userdata/` 或开发者本地配置；
  成功、失败、超时、中止与异常退出都要走清理路径，清理失败必须让测试失败或明确报告。
- 子进程、进程树、端口与文件句柄要有可观察的回收结果，不把"操作系统最终会回收"当作清理证据。
- 模块默认面向 Windows / macOS / Linux：路径用路径组件拼接、不假设某个 shell 或系统工具存在、
  stdin/stdout/stderr 使用明确编码、不依赖真实外网或真实密钥。
- 平台特有能力通过平台测试或平台探针验证；不具备条件时记 `env-skip` 或 `gap`，并写明原因与可执行的验证方法。
  确实只支持某个平台时，必须在职责或工具说明里写明限制，不能用跨平台措辞掩盖。

### 5.5 观察面

测试只根据公开且稳定的观察面判定结果：工具 stdout 的 JSON、stderr 与退出码、文件内容与目录布局、
记录型替身的调用记录、报告中的结构化字段。必须读私有状态、猜日志文案或依赖不可稳定解析的输出 = 观察面不足，
应先补稳定的输出契约或记录缺口；观察面只暴露判定行为所需的信息，不泄漏密钥与真实路径。

### 5.6 交付前检查清单

交付回报中至少说明：

- 正常、错误、边界场景分别在哪里测试；
- 用了哪些 Stub / Fake / Mock / Spy / Fixture，各自覆盖什么（语义见 [TESTING.md](TESTING.md)）；
- 工具协议或产品端口契约在哪里验证；
- 临时文件、进程、端口、日志等资源如何清理；
- Windows / macOS / Linux 的假设、已验证项与未验证项；
- 通过、缺口、环境跳过与"不适用"项的证据；
- 本次测试是否产生外部副作用，以及是否已清理。

回报中的"通过"只表示对应测试已实际执行并满足可判定验收条件；没有执行、环境不允许或尚无测试的能力，
必须分别写成 `gap`、`env-skip` 或其它适用状态，不得写成"已支持"。

## 六、供应商与密钥

**密钥归产品，不归模块。** 模块只写提示词与工具，**永不接触通道与密钥**：

- 产品维护**登记处**（`.home/`，git 忽略）：`providers.yaml`、`models.yaml`、`agents.yaml`、`settings.yaml`，
  全部由用户在产品界面里管理；字段契约见 [REGISTRY_SPEC.md](REGISTRY_SPEC.md)。
- **模块不承载模型偏好**：`module.yaml` 里**没有**模型或供应商字段。用哪个模型由用户为 **agent** 选定，选择记在会话里（只记模型 id 引用）。
- **工具的外部服务凭据（待接入）**：工具配置同样只写供应商 id，由核心拉起工具进程时解析注入；执行器已接入最小版、密钥注入尚未实现——**当前工具不得依赖任何凭据**。
- **安全红线**：密钥只存在于登记处与核心的出站调用里——永不进入提示词、转录、日志、模块工作区。
- **选择权**：用哪个供应商、哪个模型只由用户决定；模块侧不出现任何模型选择。

## 七、制作一个模块

1. 建文件夹，写 `module.yaml`（id、brief、system；**只有真需要外部程序时才写** `tools`）
2. 需要工具就把脚本放进文件夹，确认它能独立运行（参数走 stdin JSON、结果走 stdout）；
   用了非通用的解释器，就把能力名写进 `runtimes`（否则缺能力时只能看到命令失败的原始报错）
3. 单独试运行：只给 system + 一句用户需求，看它是否守边界、会用该用的东西；若声明 `tools`，再按本节测试要求验证工具协议、错误处理与清理行为
4. 按本节"交付前检查清单"整理测试证据；没有实现的测试如实登记为 `gap` 或 `env-skip`
5. 放入 `modules/`——完成。清单自动出现它，菜单自动展示它的 brief

文件读写、关键词搜索这类通用能力**不用自己造**：核心内置了 `read` / `write` / `search`（见「内置文件工具与路径模型」）——
随仓的三个示例模块（`research` / `reviewer` / `summarizer`）就只写了 `system`、一个外部工具都没声明。

没有注册、没有继承、没有编译依赖。**一个文件夹就是全部。**