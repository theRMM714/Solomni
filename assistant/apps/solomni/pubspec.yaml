name: solomni
description: 产品：装配者 + 终端交互。启动/发现/排除/外部拉起/退出是本产品的唯一策略。
environment:
  sdk: ^3.5.0

dependencies:
  core:
    path: ../../packages/core
  protocol:
    path: ../../packages/protocol
  transport:
    path: ../../packages/transport
  conversation:
    path: ../modules/conversation
  llm_gateway:
    path: ../modules/llm_gateway
  secrets:
    path: ../modules/secrets
  ui_cli:
    path: ../modules/ui_cli
