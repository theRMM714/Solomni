name: solomni
description: 核心宿主：嵌 broker + 扫描/拉起 service + 极简菜单唤起 surface + 退出。
environment:
  sdk: ^3.5.0

dependencies:
  core:
    path: ../../packages/core
  protocol:
    path: ../../packages/protocol
  transport:
    path: ../../packages/transport
