name: llm_gateway
description: LLM 网关模块：OpenAI 兼容真调用。供应商配置（地址/模型）归自己，钥匙经 prefer-shared 从 secrets 取。
environment:
  sdk: ^3.5.0

dependencies:
  protocol:
    path: ../../../packages/protocol
  transport:
    path: ../../../packages/transport
  ui_vocab:
    path: ../../../packages/ui_vocab
  user_data:
    path: ../../../packages/user_data
