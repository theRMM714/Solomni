name: secrets
description: 密钥模块：证明"密钥也只是普通模块"，无任何特权。
environment:
  sdk: ^3.5.0

dependencies:
  protocol:
    path: ../../../packages/protocol
  transport:
    path: ../../../packages/transport
  ui_vocab:
    path: ../../../packages/ui_vocab
