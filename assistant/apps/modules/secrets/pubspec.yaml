name: secrets
description: 密钥模块：钥匙圈（名字+钥匙+备注），只管钥匙不问去哪扇门；无任何特权。
environment:
  sdk: ^3.5.0

dependencies:
  protocol:
    path: ../../../packages/protocol
  transport:
    path: ../../../packages/transport
  ui_vocab:
    path: ../../../packages/ui_vocab
  user_data:
    path: ../../../packages/user_data
