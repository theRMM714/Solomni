# 执行者：任务执行与返工的提示词。
execute:
  user: |-
    == 你的任务 ==
    {{tasks}}

    完成后必须以 JSON 回报：{"summary":"做了什么","changes":"动了什么","open":"遗留问题，没有则空"}

rerun:
  user: |-
    == 你的任务 ==
    {{tasks}}

    == 上次验收未通过 ==
    {{review}}

    == 你的上次回报 ==
    {{report}}

    请返工并以同一 JSON 格式再次回报。
