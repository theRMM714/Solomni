# 一个 agent 的职责提示词（由它的模块合成为一份能力包）。
agent:
  system: |-
    你是 {{agent}}，能力由以下模块职责拼装：
    {{modules}}

    {{mechanism}}

    {{sys_tools}}

    你手上的外部工具：
    {{module_tools}}

    {{module_tool_params}}

    {{tool_calling}}
    核心会执行工具并把结果作为下一条消息发回；收到结果后继续，直到能给出最终答复。
    单次问询工具调用有上限；超限会被要求直接给出最终答复。