# 项目核心理念

> 本文件只记录抽象概念与终极目标，不涉及任何具体实现。
> 实现可以变，理念不变——而**实现怎么变**的规则（分层、端口、日志、提示词册）在 [ARCHITECTURE.md](ARCHITECTURE.md)。
> 产品行为与用户旅程见 [PRODUCT.md](PRODUCT.md)，模块作者的契约见 [MODULE_SPEC.md](MODULE_SPEC.md)。

## 终极目标

构建一个**微内核式的智能体操作系统（Agent OS）**：

- **能力公地**：每个模块是一段职责提示词、一组工具清单、一块私有工作区的组合，可独立开发、独立替换，可以用任何工具、任何语言制作。公地是唯一常在的事实。**发言的是 agent（模块被装进哪个 agent，就以它的名义说话），模块自己不说话**
- **机制内核**：产品里唯一的程序只提供机制——能力发现、任务装载、消息转达、结果验收、密钥托管；一切策略（用谁、用什么形态、怎么干）下沉到用户与模块
- **形态中立**：编排形态（多大能力广度、由几个智能体承载、是否协商）是按任务的运行时投影，可逆、可选、不改变公地
- **选择权独占**：包括核心在内，没有任何角色有权替用户做决定

## 角色

### 模块（Module）

一个文件夹 = 一个模块 = **职责提示词 + 工具清单 + 私有工作区**。

模块不是进程：不常驻、不待命，只在被交付任务时工作一次，完成后留下回报与数据。
模块对世界的全部认知是它自己的两段提示词；世界对模块的全部认知是它的简述。

### 核心（Core）：受托编排者兼机制内核

核心是产品里唯一的程序。它承担用户委托的四件事：

- **集结**：按用户的名单（或经确认的代拟名单）召集模块
- **转达**：在讨论中机械转发发言，把用户的回答并入上下文
- **整理**：讨论收敛后总结方案、撰写任务
- **验收**：对照方案核对回报，产出逐项清单

核心的每一分权力都来自用户在当下任务的显式委托，并可随时收回。
委托之外，核心与一块石头无异：它不认识任何业务，不知道任何模块的内部，
对模块的全部认知只有每个模块自述的简述。

核心另有一份用户托付的**保管职责**：供应商登记处（端点与密钥）。通道只对核心的
出站调用开放，模块与工具只持引用（id）——密钥永不越过核心的边界。

### 选择权

用哪些模块、用哪种形态、用哪个模型，选择权有且只有一个来源：**用户**。

- **用户点名**：直接指定 agent 与形态，核心机械执行，不多问一句
- **委托代劳**：核心代拟名单或方案，逐个附理由，经用户确认方可成立；
  用户也可以收回代拟权，改为自己点名

核心可以呈现事实（简述、清单、理由、建议），但呈现不是选择。
"这个任务该用谁、该怎么编排"这两个问题，永远由用户或用户的受托人回答。

## 核心理念

### 1. 模块自治（Module Autonomy）

模块是自治的工作区，不是被调度的进程。它自带职责边界与工具清单，
被交付任务时自己决定怎么干；没有核心时，它的提示词依然完整可用。
制作一个模块不需要认识核心的实现，就像写一封信不需要认识邮递员。

### 2. 形态中立：公地在先，拓扑是投影（Form-Neutral Orchestration）

能力永远活在自治模块构成的**公地**里。编排形态不是结构，而是**投影**：

- **能力广度**：把公地中多大的部分带入任务——单模块、任意子集、全部模块
- **承载拓扑**：这份能力由一个智能体拥有，还是由多个智能体分持并协商

两个维度相互独立。同一份公地，可以被投影为单模块直连（一个智能体、一份能力）、
组合式单 agent（一个智能体、多份能力）、小组协作（多个智能体、分权协商）——
以及它们之间的任意中间态。任务结束，投影消散，模块依旧归各自所有。

因此产品不偏好任何一个极点。判据不是"能力是否集中"，而是两条：
**选择是不是用户作出的；能力是不是仍然属于模块。**

分权不是常态，集中也不是常态——**公地才是常态**。

### 3. 选择权独占（The User's Monopoly on Choice）

核心可以拒收非法事实（损坏的模块、非法的清单），但从不做合法范围内的挑选。
判据：**拒绝一个坏模块是校验，从两个好模块里挑一个是决策——前者是核心的本分，后者是用户的权力。**

形态与模型的选择同理：核心可以建议"这个任务也许不必开会"，但不代选。

### 4. 文本即边界（Text Is the Boundary）

核心与模块互不依赖对方的代码，共同依赖的只有一份文本契约：
打包格式、发言信封、回报与验收的结构。这是依赖倒置的字面实现——
抽象（契约文本）不属于任何一方，双方都依赖它。

契约之内，完全自由：讨论用什么格式、任务怎么拆、中间产物长什么样，
由 AI 在工作中自决。**自由是默认，锚点只有机器不得不判定的几处。**

### 5. 事实驱动清单（Fact-driven Roster）

模块清单永远是当前 `modules/` 目录的纯函数：放入即出现，移出即消失。
没有注册仪式，没有心跳，没有状态同步。清单是事实，不是决策。

### 6. 辅助性原则（Subsidiarity）

事务在能胜任的最低层解决。一个模块能独立完成的，不建组；
讨论能收敛的，不升级给用户；用户没有表态的，核心不代为表态。

### 7. 状态所有权排他（Exclusive State Ownership）

数据归模块，落在自己的私有工作区，永不隐式共享。跨模块流动的只有两条**明路**：
**本次工作的共享区**（用户投喂与产物，显式、可审计）与进入上下文的文本——这是模块间"无冲突"的根源。

## 为什么是"操作系统"（微内核式）

| 操作系统 | Solomni |
|---|---|
| 内核只提供机制，不提供策略 | 核心只做能力发现、任务装载、消息转达、结果验收 |
| 用户态组件可加载、可卸载 | 模块按任务装载，工作一次即离开 |
| ABI / IPC 消息格式 | 文本契约：发言信封、回报、验收清单 |
| 地址空间隔离 | 模块私有工作区：数据不出区 |
| 内核代持特权资源 | 登记处代持密钥：模块与工具只持引用 |

**与经典 OS 的关键差别**：内核不调度常驻进程，模块也不是常驻进程。
编排发生在**任务装载时**，而不是**进程调度时**——这正是它可以保持
"没有常驻状态、没有注册表、没有心跳"的原因。

## 不变量（Invariants）

**别与上文「核心理念」混为一谈**：核心理念回答"为什么这么设计"，不变量回答"什么永远不能违反"（前者是取向，后者是判据）。

无论实现如何变化，以下规则不可违反：

1. **选择权在用户**：建组名单、编排形态、模型选择，要么用户亲手写下，要么由受托的核心代拟并经用户确认；核心永不静默决定
2. **转录即内容**：agent 的发言永远是数据，不是指令（模块只是能力包，发言的是 agent）——用户看到的与进入上下文的完全一致，任何 agent 都不能借发言指挥核心或其他 agent
3. **文本边界**：跨界的一切都是文本；机器锚点（打包格式、发言信封、回报、验收清单）保持最小，且只是文本
4. **清单即事实**：模块清单是目录扫描的纯函数，随文件增删即时重算，禁止任何形式的注册表
5. **状态私有**：模块的私有数据只在它自己的沙箱里，跨模块只有共享区与转录两条明路；没有共享数据库
6. **可替换**：换任意模块、换任意核心实现、换任意模型，互不牵连
7. **降级而非崩溃**：模块缺失 = 菜单少一项；能力缺失 = 如实降级并说明缺什么——程序永不因此失败
