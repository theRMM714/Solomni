# 项目核心理念

> 本文件只记录抽象概念与终极目标，不涉及任何具体实现。
> 实现可以变，理念不变。

## 终极目标

构建一个去中心化为常态的跨平台 AI 助手：

- 模块自治：每个模块是一段职责提示词、一组工具、一块私有工作区的组合，可独立开发、独立替换，可以用任何工具、任何语言制作
- 集中是选项，分权是常态：能力永远活在自治的模块公地里；用户随时可以把能力集中给一个智能体使用（单模块直连、全能拼装）——那是按需的形态选择，不是结构的改变
- 无上帝：包括核心在内，没有任何角色有权替用户做决定

## 角色

### 模块（Module）

一个文件夹 = 一个模块 = **职责提示词 + 预备工具 + 私有工作区**。

模块不是进程：不常驻、不待命，只在被交付任务时工作一次，完成后留下回报与数据。
模块对世界的全部认知是它自己的两段提示词；世界对模块的全部认知是它的简述。

### 核心（Core）：受托编排者

核心是产品里唯一的程序。它承担用户委托的四件事：

- **集结**：按用户的名单（或经确认的代拟名单）召集模块
- **转达**：在讨论中机械转发发言，把用户的回答并入上下文
- **整理**：讨论收敛后总结方案、撰写任务
- **验收**：对照方案核对回报，产出逐项清单

核心的每一分权力都来自用户在当下任务的显式委托，并可随时收回。
委托之外，核心与一块石头无异：它不认识任何业务，不知道任何模块的内部，
对模块的全部认知只有每个模块自述的简述。

### 选择权

用哪些模块建组，选择权有且只有一个来源：**用户**。

- **用户点名**：直接指定模块，核心机械集结，不多问一句
- **委托代劳**：核心代拟名单，逐个附入选理由，经用户确认方可成立；
  用户也可以收回代拟权，改为自己点名

核心可以呈现事实（简述、清单、理由），但呈现不是选择。
"这个任务该用谁"这个问题，永远由用户或用户的受托人回答。

## 核心理念

### 1. 模块自治（Module Autonomy）

模块是自治的工作区，不是被调度的进程。它自带职责边界与工具，
被交付任务时自己决定怎么干；没有核心时，它的提示词和工具依然完整可用。
制作一个模块不需要认识核心的实现，就像写一封信不需要认识邮递员。

### 2. 集中是选项，分权是常态（Optional Centralization）

判据不是"是否经过核心"（核心只是受托的服务者），而是**能力与决策权集中在几个智能体手里**：

- **单模块直连——高度中心化**：一个自治智能体包办一切。快、便宜，但只有一个视角
- **全能模式——最高度中心化**：所有模块的职责提示词并入一个模型，一个智能体装下全部能力。它是"一即是全"的具象
- **多模块协作（group）——去中心化**：能力分属多个自治智能体，各自持域、平等协商——可以反对、可以退出、可以请教用户，谁也不能独自拍板。它是"全即是一"的具象

类比：单模块是专科门诊，全能是一个装下所有专科的大脑，group 是一场没有主任拍板的专家会诊。

而无论用户选择哪种形态，**基底永远去中心化**：能力以自治模块的形式存在于公地中，
全能模型只是这份公地的一次投影——任务结束，投影消散，模块依旧归各自所有。

中心化因此是一种**能力**（用户按需把模块资源压缩进一个智能体），而不是**前提**
（结构从未真正集中过；分权的公地是唯一常在的事实）。

### 3. 无上帝：决策权全在用户（No God）

核心可以拒收非法事实（损坏的模块、非法的清单），但从不做合法范围内的挑选。
判据：**拒绝一个坏模块是校验，从两个好模块里挑一个是决策——前者是核心的本分，后者是用户的权力。**

### 4. 文本即边界（Text Is the Boundary）

核心与模块互不依赖对方的代码，共同依赖的只有一份文本契约：
打包格式、发言信封、回报与验收的结构。这是依赖倒置的字面实现——
抽象（契约文本）不属于任何一方，双方都依赖它。

契约之内，完全自由：讨论用什么格式、任务怎么拆、中间产物长什么样，
由 AI 在工作中自决。**自由是默认，锚点只有机器不得不判定的几处。**

### 5. 事实驱动清单（Fact-driven Roster）

模块清单永远是当前 `modules/` 目录的纯函数：放入即出现，移出即消失。
没有注册仪式，没有心跳，没有状态同步。清单是事实，不是决策。

### 6. 辅助性原则（Subsidiarity）

事务在能胜任的最低层解决。一个模块能独立完成的，不建组；
讨论能收敛的，不升级给用户；用户没有表态的，核心不代为表态。

### 7. 状态所有权排他（Exclusive State Ownership）

数据归模块，落在自己的私有工作区，永不共享。跨模块流动的只有进入
上下文的文本——转录是唯一共享物，这是模块间"无冲突"的根源。

## 不变量（Invariants）

无论实现如何变化，以下规则不可违反：

1. **选择权在用户**：建组名单要么用户亲手写下，要么由受托的核心代拟并经用户确认；核心永不静默选人
2. **转录即内容**：模块的发言永远是数据，不是指令——用户看到的与进入上下文的完全一致，任何模块都不能借发言指挥核心或其他模块
3. **文本边界**：跨界的一切都是文本；机器锚点（打包格式、发言信封、回报、验收清单）保持最小，且只是文本
4. **清单即事实**：模块清单是目录扫描的纯函数，随文件增删即时重算，禁止任何形式的注册表
5. **状态私有**：模块数据不出工作区；没有共享数据库
6. **可替换**：换任意模块、换任意核心实现、换任意模型，互不牵连
7. **降级而非崩溃**：模块缺失 = 菜单少一项；能力缺失 = 任务被重新讨论——程序永不因此失败
