# 产品与用户旅程

> 本文档回答：Solomni 是什么、有什么功能、怎么运转。
> 只写**用户看到与经历的**，不展开实现；实现与开发规则见 [ARCHITECTURE.md](ARCHITECTURE.md)，
> 模块契约见 [MODULE_SPEC.md](MODULE_SPEC.md)，登记处字段见 [REGISTRY_SPEC.md](REGISTRY_SPEC.md)，理念见 [PHILOSOPHY.md](PHILOSOPHY.md)。

## 产品是什么

**一个可执行的核心 + 一个 `modules/` 文件夹 = 整个产品。**

- **核心（编排者兼机制内核）**：产品里唯一的程序。它认识模块的方式只有一种：读每个模块文件夹的
  `module.yaml`。它不含任何业务——业务全部在模块里。
- **模块**：一个文件夹 = 一个模块 = 一段职责提示词 + 可选的外部工具 + 一块私有工作区。
  **模块不是进程**：不常驻、不联网等待，只在被交付任务时工作一次，完成后只留下回报与数据。
- **安装 = 放入文件夹；卸载 = 移出文件夹。** 模块清单在每次启动、每次建组时重新扫描：
  放入即出现，移出即消失，无需重启。

## 模块由什么构成

一个文件夹就是一个模块：

- `brief` —— 给**选人**看的能力简述（两三句，只说「能干什么」）
- `system` —— 给**被交付任务的 AI** 看的职责提示词（管辖边界 + 工具用法）
- `tools` —— **可选**的外部工具表（文件读写这类通用能力，用核心内置的就好）
- `userdata/` —— 模块自己的跨任务私有区：数据不出这里，git 忽略

铁则：**brief 不承诺细节，system 不越界，模块数据不出模块自己的私有区。** 打包格式与全部机械约定见 [MODULE_SPEC.md](MODULE_SPEC.md)。

## 供应商、模型与密钥（产品层）

供应商与模型是**产品级资源**，分开保存、由用户经界面管理：

- `providers.yaml`——供应商通道：端点 + 密钥。（`.home/`，git 忽略）
- `models.yaml`——模型：id、展示名、实际模型串（发给供应商的那个）、所属供应商、能力说明；
  以及**核心 AI 默认模型**。模型 → 供应商的绑定只对本产品可见。
- `agents.yaml`——agent 登记处：名字、它具备的模块、它的默认模型、一句说明（见下文「agent：能力的最小承载单位」）。
- **模型选择发生在创建工作前**：用户逐个 agent 指定模型，
  也可以让核心按模块简述与需求**推荐**（附理由，用户可改）。选择随会话落盘，只记模型 id 引用。
- 密钥只在两处存在：providers.yaml、核心发起的出站调用。永不进入提示词、转录、日志、模块工作区。
- **讨论是"轮流发言"，不是"每轮轮询"**：发过 `agree` 的人不再被追问（同意**粘住**），
  直到在场者全部同意；`leave` 的人不算在场。开场发言与轮次发言同一口径（表态立刻生效）。
- 「用哪个模型」是用户的权力；未指定时回落核心默认；都缺 = 演示通道并如实告知，绝不静默。
- **流式与超时是全局设置**（`streaming` / `llm_timeout_secs`）：讨论、执行、验收与单 agent 共用同一份。
  流式开关是**上限**——关掉时一律非流式；打开时调用方仍可在本次放弃流式（回包形状的选择）。
  单次调用的预算用尽时，**中断这一轮**并如实说明原因（含预算值），会话保持可继续——点「继续」即重试；
  失败**不会被当成模型发言**写进转录（转录是核心派生「下一步该谁说话」的依据，混进错误文本会让状态歪掉）。
- 登记处当前只管一类通道：**LLM 供应商**。工具调用的外部服务托管尚未接入（见"后置工作"）。
- 四份 yaml 的字段与读写规则见 [REGISTRY_SPEC.md](REGISTRY_SPEC.md)；密钥永不回显给界面。

## 分层与决定权

**核心不替用户选人、不替用户选形态、不替模块干活、不替验收放水。** 分层与依赖方向、端口与日志见 [ARCHITECTURE.md](ARCHITECTURE.md)。

## 编排平面：两个旋钮，两个形态

编排形态由两个**相互独立**的维度决定：

| 能力广度 ＼ 承载拓扑 | 单一智能体（集中） | 多智能体（分权协商） |
|---|---|---|
| 单模块 | **直连式**（单 agent，1 个模块） | —（无协商对象） |
| 任意子集 | **组合式**（单 agent，多模块） | **小组协作**（N 个 agent） |
| 全部模块 | **组合式（全选）** | **全员协作** / `collab ?`（核心代拟名单） |

- **能力广度**：单模块 → 任意子集 → 全部模块（子集是**一等公民**，不是特例）
- **承载拓扑**：一个智能体拥有这份能力（集中），或多个智能体分持并协商（分权）

产品层面只给**两个形态**（`mode` 只有这两个值）：

| 形态 | agent 数 | 适合 |
|---|---|---|
| **单 agent** `single` | 恰好 1 个（模块数不限） | 任务清晰或轻量跨域：极快极省，一个模型装下多份能力，不开讨论 |
| **协作** `collab` | N 个（各自独立、各自沙箱） | 目标模糊、跨域、需要多视角互检或多种工具 |

「1 个模块 = 直连式、多个模块 = 组合式」是同一形态里的两个刻度（向导里的提示），不是三种模式。

成本与延迟：**同一广度下** 单智能体 < 多智能体；**同一拓扑下** 广度越大越贵。
越集中越快越省但视角单一，越分权越慢越贵但视角更全。形态由用户选择；核心可以建议，不代选。

### agent：能力的最小承载单位

**只有 agent 发言。** 模块是能力包（职责提示词 + 工具 + 文件），agent 是发言人；模块被装进哪个 agent，就以那个 agent 的名义说话，
转录里的说话人、沙箱目录名、同意/撤回/回报的归属，全都是 agent 实例名——不存在「模块发言」这回事。

形态选好后，用户选的是 **agent**（不是零散的模块）：

- 一个 agent = 一个名字 + 它具备的模块 + 它的模型 + 它自己的沙箱 → 一个独立的 AI 实例
- 单 agent = 恰 1 个 agent（**1 到多个模块**，多模块合成一个 AI）；协作 = N 个 agent（各自独立、各自沙箱）
- agent 存在 `.home/agents.yaml`（名字 / 模块 / 模型 / 说明），在设置里管理；建工作时可**临时用**（transient，不写登记处），也可顺手存成常驻 agent
- 同一个模块在一次工作里只能属于一个 agent（否则沙箱与发言归属会歧义，核心直接拒绝）；agent 重名时核心自动加尾号 `-2`、`-3`，并先让用户裁决是否改名
- 核心可按需求推荐 **agent 草案**：**先看登记处里有没有合适的 agent——有就复用**（模块与模型都照它自己的，核心不拟模型），
  没有才帮忙**组装**一个（这时才附上模型建议）。核心只是把表单填好，确认必须由用户点
- 两个形态共用同一会话内核：单 agent 与协作里的每个发言席，都是同一份「一个 agent 说话」的实现

### 工作的落盘与沙箱

```text
session/<工作名>/
  meta.yaml          # 形态、agent 清单、模块与模型选择、需求、执行档位（exec 段）
  transcript.jsonl   # 追加式事件流水（回档只追加一条 rewind 记录）
  work/              # 本工作共享区：用户投喂的文件与成品
  <agent 实例名>/     # 该 agent 的私有沙箱
```

- agent 的文件读写用核心自带的**内置工具**（`read` / `write` / `edit` / `patch` / `search`；`patch` 用自由格式补丁，大段内容不必塞进 JSON）：核心在提示词里把**占位符替换成运行时的真实根目录**，agent 拿到的就是真实绝对路径；越出这些根（相对路径、`..` 跳出）一律拒绝；模块目录只有它所属的 agent 可达（细节见 [MODULE_SPEC.md](MODULE_SPEC.md)）
- **执行档位**（`exec` 段）：**本机档**（默认）= 脚本直接在宿主上跑；**虚拟机档** = 一整套 guest。
  虚拟机档的选型、诊断与装配计划已就位，**guest 本体尚未接入**——未接入前实际仍按本机档执行，产品如实标注，不夸大
  （见"后置工作"与 [RUNTIME_SPEC.md](RUNTIME_SPEC.md)）。
  **档位承载按逐项前置判，且现在一律不可用**：虚拟机档要满足四件事——虚拟机监视器、guest 本体、QEMU、基础根。
  其中**guest 本体尚未接入是所有机器共同缺的一项**，所以此刻虚拟机档**任何机器都不允许创建或改入**，
  配置界面直接禁用并**逐项列出缺什么、每项怎么补**（怎么开虚拟化、QEMU 从哪来、镜像从哪来）。
  理由：此刻的"虚拟机档"只会得到一个**比本机档更差**的东西（隔离一点没多，缺包模块的工具反而被拒），创建它没有意义。
  QEMU 与镜像**用户自备**，产品只检测与指路（不自带、不下载）——不背分发义务、仓库也不膨胀。
  **已存在的会话不拦打开**（记录是用户的）：记的是虚拟机档而现在承载不了时，会话列表标注"虚拟机档不可用"，
  打开时主动弹窗说明原因与出路，本次仍按本机档执行
- **运行能力**：模块用自己的 `runtimes` 声明工具需要哪些运行包（`python` / `node` / `bash` / `cc`……），包放在依赖文件夹 `runtimes/` 里，
  放入即出现。缺包 = 该模块的工具不执行并如实说明缺什么，**不是**会话崩溃（契约见 [RUNTIME_SPEC.md](RUNTIME_SPEC.md)）
- 隔离级别如实：三层各说各的**实际**强度——① 内置 read/write/edit/patch/search 是**工具层**越界拒绝（路径校验）；
  ② 外部工具进程统一从**守门进程**里跑（本程序自己的 `--fence-run` 模式），环境白名单（密钥不进子进程、HOME/TEMP 落在私有沙箱）、
  超时连根杀掉整棵进程树、Windows 上用 Job Object 围住进程树；③ 文件系统与网络围栏按平台接入：Linux（Landlock）、macOS（seatbelt）、
  Windows（AppContainer：按 agent 派生容器 SID、可达范围凭目录 ACL 授权、不给 capability 即断网）。
  **启动时会对本机做一次自检并如实报告能力等级**——机制装不上就说装不上（例如受限环境里不允许改目录 ACL 或建容器 profile）。
  **未授权不等于无围栏**：没授权时段，路径级文件系统围栏与断网是关的（它们要写目录 ACL 才装得上），
  但**进程树围栏、进程数上限与环境白名单照旧生效**——启动报告把"本机能力"与"本次实际"分两行说清，不让人误读。
  机制自检分三态：本机不允许（内核不支持、私有 ABI 失效、系统拒绝建容器）如实降级照跑；
  **自检已确认机制有效却仍装不上 = 程序自己写错了，那一路拒绝执行**（命令不落进程，工具回执如实说明），
  绝不按"无围栏"跑——否则用户以为有围栏、实际什么都没有。
  三平台围栏都已在各自平台的真机上跑过探针（Windows AppContainer + 目录 ACL、Linux Landlock、macOS seatbelt），
  探针在 `tests/windows/`、`tests/linux/`、`tests/macos/`，缺口账现状见 [TESTING.md](TESTING.md) 与 `docs/testing/`
- **围栏要用就征得同意**：Windows 上的路径级围栏必须在本机目录上写权限项，所以本程序**默认不写**——只有显式授权（`.home/settings.yaml` 的 `fence_write: true`，或环境变量 `SOLOMNI_FENCE_WRITE=1`）才做；授权后每次写入都在 stderr 逐条列出并记进授权台账；`solomni --fence-clean` 按台账精确撤销并删掉建过的容器 profile（删除会话时也会撤销该会话的授权）
- 上传同名文件绝不静默覆盖（弹出覆盖 / 改名 / 取消）

## 运行流程

### 启动

```text
$ start.bat                 # Windows（或 node start.js）；macOS/Linux 用 ./start.sh
[发现] harvest · indexer · render           <- 扫描 modules/，只报事实
（菜单：编排入口 + 模块清单，brief 在此给用户看）
```

启动层序负责工具链与构建（细节见 [README.md](README.md)），核心启动后立即扫描 `modules/`。

### 协作（`collab <agent名,…>`）：一次完整流程（六步）

任务示例：「调研三款开源数据库，选一款并入我的知识库，写好引入笔记。」

```text
（1）建组
用户：下达需求（可点名 agent，或什么都不说）
核心（受托代拟）：先看登记处里有没有合适的 agent——有就复用（模块与模型都照它自己的）；没有才帮忙组装一个（这才附模型建议）
　　　　　　　　　例：调研员（复用，模型 m1）＋ 新助手〈db,notes〉→ m2（理由）
用户：确认名单 —— 「yes」按名单开始；「yes,allow」开始且授权小组自裁细节，不再逐轮请示
（确认的那一刻名单写回会话 meta；重启/回档后照它重建，不靠转录猜）

（2）讨论
核心把「聊天约定 + 各 agent 的职责提示词（其模块 system 的合成）+ 用户需求」拼成首轮提示词，发给第一个 agent；
其后把已有发言并入上下文逐个转达，一轮走完所有在组 agent。
轮与轮之间核心向用户确认「有无补充」：有意见则并入上下文，yes 继续。
agent 可以退出（此后核心不再向它转达）；可以请教用户（核心转达，回答并入上下文）。
终止条件：留下的 agent 全部表示同意 —— 或触及轮次上限（超限交用户裁决）。

（3）整理
全员同意后，核心介入：总结讨论，并把它拆成一张**任务链**——每个节点 = 一份任务提示词 + 负责人 + 依赖。
能一起做的节点互相不依赖（会并发开工）；要等别人全部结束的，把依赖列上。

（4）审查关卡
整理完**不自动开工**：方案与链先交给用户看，点「同意」才推进。
（细则见 docs/architecture/task-chain.md）

（5）链驱动
就绪的节点各自起一个**子会话**：普通单 agent 会话，沙箱与父会话共用同一个工作区。
agent 在自己的子会话里按模块的工具清单干活、交付回报（回报 = 做了什么 / 动了什么 / 遗留什么）。
子会话跑完自动叫醒父会话，推进下一个节点。
用户能点进任意子会话看进展，也能在子会话里发言影响那个 agent。

（6）验收
**节点级（每阶段一次）**：本阶段节点都跑完后，核心按每个节点的**当前目标**判它的产出；没过 → 暂停并告知
是**哪几个**节点，点「继续」重派它们（子会话复用，agent 看得到自己上次的产出；同阶段已通过的保持通过）。
全部阶段落定 → **总验收**：核心对照方案逐项核对各节点产出，产出 pass/fail 清单。
全部 pass → 交付用户；有 fail 的条目**必须点名要返工的节点**（`checklist.items[].rework` = 节点 id）——
核心只把点名的节点退回待办，点「继续」后只重派它们（**不交付**）；填错或漏填则由核心要求重填。
```

退出即静默、同意须全员、请教必转达、上限必生效——这四条行为不变，
发言用什么记号、信封长什么样，由模块契约文档定义，本文不规定。

### 单 agent（`single`）

```text
> single harvest             # 直接用登记处的 agent（模块数不限）
> single harvest notes       # 把点名 agent 的模块并成一个临时 agent
> single                     # 登记处全部 agent 的模块并成一个（全选）
（上下文 = 该 agent 的职责提示词（其模块 system 的合成）+ 用户消息，直接对话干活；
  内置 read/write/edit/patch/search 随时可用，其模块声明的外部工具也可用；每次调用以工具卡片如实呈现）
```

工具的身份是 (模块, 工具名)：不同模块可以声明同名工具，调用时在信封的 module 字段里指明，各归各模块——**合成一个 agent 不再有资源冲突这回事**。

### 退出

| 事件 | 语义 |
|---|---|
| 关闭产品 / Ctrl+C | 核心进程消亡，产品退出；进行中的任务随之中止 |
| userdata | 已落盘的数据保留，下次启动照常 |

## 交互面：转录中心

两种交互面，呈现同一份转录（终端与本地网页并列）：

- **终端（默认）**：菜单、讨论、请教点、执行、验收，逐行渲染
- **本地网页（`-webUI`，或在终端输入 `webui`）**：只绑 `127.0.0.1`（默认端口 3081），
  增量刷新、断线重连；PC 双栏布局，移动端抽屉式侧栏

两者呈现五样东西：

- **菜单**：模块清单（brief，能力公地）与编排入口（选 agent）
- **讨论**：谁在发言、谁退出了、谁已同意、第几轮
- **请教点**：agent 的问题与用户的回答，原样进上下文
- **执行**：各 agent 任务进度与回报
- **验收**：pass/fail 清单

### 转录的结构：一轮一块，工具调用自成一块

一次用户发言可能包含**多轮模型调用**（模型申请工具 → 核心执行 → 把结果发回 → 模型再说）。转录按**到达顺序**如实分块：

```text
思考（折叠）
这一轮的输出
┌ 工具调用：模块.工具名 ✓   ← 参数与结果折叠在里面，点开才看
└
思考（折叠）
这一轮的输出
┌ 工具调用：模块.工具名 ✓
└
最后一轮的思考与输出
```

每一块都能单独回档（删掉这一块及其后），工具调用自成一块、不混进普通消息里。
**落盘的转录即上下文**：重启或回档后续跑时，工具调用与工具结果都还在上下文里（机制见 [ARCHITECTURE.md](ARCHITECTURE.md)）。

**安全边界**：转录中心呈现的 agent 发言永远是**内容**，不是指令——用户看到的与进入
上下文的完全一致，任何一个 agent 都不能借发言指挥核心或其他 agent。

### 会话与历史（转录中心）

- **创建工作**：填工作名 → 选形态（单 agent / 协作） → 选 agent（或勾模块组一个） → 逐个 agent 指定模型
  → 协作还要写下本次需求。工作名即落盘目录名。
- **落盘**：每个工作一个目录 `session/<工作名>/`（内容见上文「工作的落盘与沙箱」）。记录**只追加**、回档也是追加一条记录而不是物理删除，
  所以审计留得住；会话内容以最后一个回档点为准（机制见 [ARCHITECTURE.md](ARCHITECTURE.md)）。
- **历史**：侧栏列出会话，每一项给三个动作——**打开**（进会话视图：转录 + 继续/停止）、**编辑**（进配置视图）、
  **删除**（✕，记录永久删除；当前**不**先列出工作区里还剩哪些文件，见 `tests/gaps.yaml` 的 `session.delete-without-file-check`）；
  点条目本身不直接进入，避免误开。
- **配置视图**（进入会话视图之前）：改每个 agent 的模块与模型、执行档位、能力定版、网络开关；
  同一模块不得同属两个 agent；**会话一旦开过（流水里有内容）agent 名单与形态就冻结**（要换人请新建会话）；
  正在生成中不许改（先停止或等它结束）。缺运行包、拒收的模块与包、虚拟机档的诊断都在这里如实列出，
  并给出依赖文件夹路径——放好包点「重新扫描」即可。每次提交追加一条**旁路配置记录**（只作呈现与审计，不进模型上下文）。
- **回档**：点某条消息的「删除此行和之后所有消息」= **把该行连同其后全部回退**（点第一行即清空转录）。
  **回档只改会话，不改世界**：已经花掉的模型调用与已经发生的工具副作用不会回滚；跨过工具执行的回档会明确提示。
- **引用文件**：输入框里打 `@` 会列出**本次工作共享区**（`work/`）与各 **agent 私沙**（`<agent>/`）里已有的文件，
  选中后插入 `@work:notes.txt` / `@sandbox:专属/out.md`。**核心把引用改写成 agent 能用的确切寻址**
  （**运行时的真实绝对路径**）后才进入转录与上下文——所以转录里看到的就是 agent 实际拿到的；
  引用了**别的 agent 私有沙箱**里的文件时，核心如实说明当前 agent 无权读取（不泄漏它的真实路径），**不偷偷复制到共享区**。
  界面把落在本次工作根目录下的长路径**显示**成短式胶囊（`work/相对路径` / `沙箱 <agent>/相对路径`，悬停看完整路径）——
  只改显示，转录里存的仍是完整路径（转录即内容不变）。
- **停止**：生成中「发送」键变为「停止」，点了即中止：立刻收尾、保留已产出的部分，生成中那些临时的即时播报一并作废。
- **继续**：由用户点击授权。单 agent 形态需要轮到你发言——末条是 AI 时会提醒而不是硬发请求；
  协作不需要用户发言，直接从断点推进流水线（重启后打开历史会话也能继续）。
- **撤回同意**：协作里某个 agent 的 `agree` 可被单独撤回，核心按剩余转录重新判定收敛。
- **基本设置**（面板写回 `.home/settings.yaml`，字段全表见 [REGISTRY_SPEC.md](REGISTRY_SPEC.md)）：
  **流式传输**（供应商逐片返回，边收边显示）、**思维链显示**（渲染在回答上方，**永远默认折叠、点击才展开**）、
  **单次模型调用的超时**（全局一份预算）、**一轮内最多提醒几次**、**上下文用到百分之多少就压缩**。
- **回档/继续/改需求只改会话，不改世界**；生成过程中的即时播报不写进历史，历史只记定稿后的内容。

## 模块清单动态性

- 每次启动、每次建组都重新扫描 `modules/`
- 文件夹缺失或损坏的模块不进清单：缺能力 = 菜单少一项，不是崩溃
- `module.yaml` 非法的模块拒收并说明原因——校验，而不是挑选

## 后置工作

**测试相关的未完成项不在本文维护**：唯一真相是 `tests/gaps.yaml`（全局）与 `tests/<平台>/gaps.yaml`（平台），
入口与判定见 [TESTING.md](TESTING.md) 与 `docs/testing/`。

产品侧仍待接入的能力：

- **虚拟机档 guest 本体**：会话级 guest + 运行包按声明装配（`prefix` 类只读共享、`system` 类叠层并预检冲突）。
  选型、诊断、装配计划与**逐项前置检测**已就位（见 [RUNTIME_SPEC.md](RUNTIME_SPEC.md)）；guest 未接入前虚拟机档一律不可选。
  **方向已定**：走平台原生虚拟化（Windows 虚拟机平台 / Linux KVM / macOS 虚拟化框架），**不内置 QEMU、不随仓库分发镜像**
  （QEMU 与镜像用户自备，产品只检测与指路）——这样不背 GPL 源码分发义务、仓库也不膨胀。
  **推迟理由**：guest 相对现有围栏的独特价值只有"不授权本机写权限也能拿到强隔离"一条，而代价是三套原生 API、
  每平台真机验证，且 CI 起不了 hypervisor（只能真机验 + env-skip）。等项目成熟、需求更清楚时再做。
- **工具通道托管**：登记处同时管理工具调用的外部服务凭据（模块只持 id）
- 混合拓扑：部分能力合成一个 agent、其余分权协商（当前是「单 agent」与「N agent」两个端点）
- 执行期并行调度（无依赖任务同时跑）与讨论上下文压缩
- 模块分发：打包、签名、市场
- 供应商管理界面：密钥轮换、用量统计
