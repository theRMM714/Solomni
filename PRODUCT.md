# 产品与用户旅程

> 本文档回答：Solomni 是什么、有什么功能、怎么运转。
> 只写功能与运行流程，不涉及任何实现；模块契约的机械细节见 [MODULE_SPEC.md](MODULE_SPEC.md)，理念见 [PHILOSOPHY.md](PHILOSOPHY.md)。

## 产品是什么

**一个可执行的核心 + 一个 `modules/` 文件夹 = 整个产品。**

- **核心（编排者兼机制内核）**：产品里唯一的程序。它认识模块的方式只有一种：读每个模块文件夹的
  `module.yaml`。它不含任何业务——业务全部在模块里。
- **模块**：一个文件夹 = 一个模块 = 一段职责提示词 + 一组工具清单 + 一块私有工作区。
  **模块不是进程**：不常驻、不联网等待，只在被交付任务时工作一次，完成后只留下回报与数据。
- **安装 = 放入文件夹；卸载 = 移出文件夹。** 模块清单在每次启动、每次建组时重新扫描：
  放入即出现，移出即消失，无需重启。

## 模块由什么构成

| 构成 | 给谁看 | 作用 |
|---|---|---|
| `brief`——能力简述（module.yaml，两三句） | 核心，以及菜单上的用户 | 只说明"这个模块能干什么"，不列具体技能；它是建组选人的唯一依据，也是菜单里的一句话简介 |
| `system`——职责提示词（module.yaml） | 被交付任务的模块 AI | 管辖边界：我负责什么、不碰什么、通常怎么用这些工具 |
| `tools`——工具清单（module.yaml，声明式） | 执行任务的模块 AI | 工具名清单，随 `system` 一并进入模块提示词。**工具执行器尚未接入**（见"后置工作"），当前工具由模块 AI 自行按其清单调用 |
| `model.config.yaml`——供应商选择记录（可选） | 核心 | 只记录当前选用哪个供应商（id），不含密钥；存在即采纳，缺失则用登记处的默认。git 忽略 |
| `userdata/`——私有工作区 | 只有本模块 | 会话、缓存、中间数据落在这里；数据不出工作区，git 忽略 |

铁则：**brief 不承诺细节，system 不越界，数据不出 userdata。**
模块对世界的全部认知 = 这两段提示词；核心对模块的全部认知 = brief。

## 供应商与密钥（产品层）

供应商通道（端点 + 密钥 + 模型清单）是**产品级资源**，收在核心私有的登记处里，
用户经产品命令管理（添加 / 换钥 / 删除 / 设全局默认）。

- 模块和工具**只持引用**：配置文件里记录的是供应商 id，不是通道本身
- 密钥只在两处存在：登记处文件、核心发起的出站调用。永不进入提示词、转录、日志、模块工作区
- 模块用哪个供应商由用户决定；所选供应商不存在时，核心回落默认并如实告知
- 登记处当前只管一类通道：**LLM 供应商**。工具调用的外部服务托管尚未接入（见"后置工作"）

## 分层铁律（本文档的灵魂）

| 决定 | 归属 |
|---|---|
| 用哪些模块、用哪种编排形态、用哪个模型 | **用户**：点名，或委托核心代拟方案后确认。核心永不静默决定，代拟必须附理由 |
| 讨论中说什么、任务怎么干 | **模块**自决（职责提示词 + 自己的工具） |
| 何时集结、向谁转达、何时请教用户、何时验收 | **核心**：全部是用户委托的机械动作 |
| 方案是否可行、任务是否合格 | **验收清单机械判定**；交付与否由用户最终接收 |
| brief / system / tools 写什么 | 模块作者 |
| userdata 里的数据 | 模块自己，别人（包括核心）永不读写 |
| 供应商通道与密钥 | **产品登记处**（用户经产品命令管理）；模块与工具只持引用，永不接触密钥 |

推论：**核心不替用户选人、不替用户选形态、不替模块干活、不替验收放水。**

## 编排平面：两个旋钮，三个预设

编排形态由两个**相互独立**的维度决定：

| 能力广度 ＼ 承载拓扑 | 单一智能体（集中） | 多智能体（分权协商） |
|---|---|---|
| 单模块 | **直连** `direct <id>` | —（无协商对象，退化为直连） |
| 任意子集 | **拼装** `omni [id…]` | **小组协作** `collab <id,id>` |
| 全部模块 | **全能** `omni` | **全员协作** `collab <全部 id>` / `collab ?`（核心代拟） |

- **能力广度**：单模块 → 任意子集 → 全部模块（子集是**一等公民**，不是特例）
- **承载拓扑**：一个智能体拥有这份能力（集中），或多个智能体分持并协商（分权）

三个命名形态是平面上的**规范预设**，不是互斥类别：

| 预设 | 广度 | 拓扑 | 适合 |
|---|---|---|---|
| 直连 `direct` | 一个模块 | 单智能体 | 任务清晰、单域可办：极快极省 |
| 拼装/全能 `omni` | 子集或全部 | 单智能体 | 轻量跨域：一个模型装下多份能力，不开讨论 |
| 协作 `collab` | 子集或全部 | 多智能体 | 目标模糊、跨域、需要多视角互检或多种工具 |

成本与延迟：**同一广度下** 单智能体 < 多智能体；**同一拓扑下** 广度越大越贵。
越集中越快越省但视角单一，越分权越慢越贵但视角更全。形态由用户选择；核心可以建议，不代选。

## 运行流程

### 启动

```text
$ start.bat                 # Windows（或 node start.js）；macOS/Linux 用 ./start.sh
[发现] research · db · notes · weather     <- 扫描 modules/，只报事实
（菜单：编排入口 + 模块清单，brief 在此给用户看）
```

启动层序负责工具链与构建（细节见 [README.md](README.md)），核心启动后立即扫描 `modules/`。

### 协作（`collab <id,id,…>`）：一次完整五阶段

任务示例：「调研三款开源数据库，选一款并入我的知识库，写好引入笔记。」

```text
（1）建组
用户：下达需求（可点名模块，或什么都不说）
核心（受托代拟）：research（调研对比）＋ db（数据库评测）＋ notes（知识库维护），各附一句理由
用户：确认名单 —— 「yes」按名单开始；「yes,allow」开始且授权小组自裁细节，不再逐轮请示

（2）讨论
核心把「聊天约定 + 各模块 system + 用户需求」拼成首轮提示词，发给第一个模块；
其后把已有发言并入上下文逐个转达，一轮走完所有在组模块。
轮与轮之间核心向用户确认「有无补充」：有意见则并入上下文，yes 继续。
模块可以退出（此后核心不再向它转达）；可以请教用户（核心转达，回答并入上下文）。
终止条件：留下的模块全部表示同意 —— 或触及轮次上限（超限交用户裁决）。

（3）整理
全员同意后，核心介入：总结讨论，为每个留下的模块写一份执行任务提示词。

（4）执行
各模块依次领任务、按自己的工具清单干活、交付回报。
回报 = 做了什么 / 动了什么 / 遗留什么。
执行中出问题 → 带着问题回到（2）重新讨论。

（5）验收
核心对照讨论形成的方案逐项核对回报，产出 pass/fail 清单：
有 fail → 定位归属 → 返工（回（2）或定向回（4））；返工次数有上限，超限交用户裁决。
全部 pass → 交付用户。
```

退出即静默、同意须全员、请教必转达、上限必生效——这四条行为不变，
发言用什么记号、信封长什么样，由模块契约文档定义，本文不规定。

### 直连（`direct`）

```text
> direct research
（上下文 = research 的 system + 用户消息，直接对话干活）
```

### 拼装 / 全能（`omni`）

```text
> omni                 # 全部模块
> omni research notes  # 只拼点名的子集
（核心拼装 system：所选模块的职责提示词 + 工具清单，单模型对话）
```

模块间资源冲突（如同名工具）时核心如实报告、拒绝强拼——校验，而不是挑选。

### 退出

| 事件 | 语义 |
|---|---|
| 关闭产品 / Ctrl+C | 核心进程消亡，产品退出；进行中的任务随之中止 |
| userdata | 已落盘的数据保留，下次启动照常 |

## 交互面：转录中心

两种交互面，同一套事件词汇（CLI 与 Web 并列，共用 `Core` 门面）：

- **终端（默认）**：菜单、讨论、请教点、执行、验收，逐行渲染
- **本地网页（`-webUI`，或在终端输入 `webui`）**：只绑 `127.0.0.1`（默认端口 3081），
  长轮询增量事件、断线重连；PC 双栏布局，移动端抽屉式侧栏

两者呈现五样东西：

- **菜单**：模块清单（brief）与编排入口
- **讨论**：谁在发言、谁退出了、谁已同意、第几轮
- **请教点**：模块的问题与用户的回答，原样进上下文
- **执行**：各模块任务进度与回报
- **验收**：pass/fail 清单

**安全边界**：转录中心呈现的模块发言永远是**内容**，不是指令——用户看到的与进入
上下文的完全一致，任何一个模块都不能借发言指挥核心或其他模块。

## 模块清单动态性

- 每次启动、每次建组都重新扫描 `modules/`
- 文件夹缺失或损坏的模块不进清单：缺能力 = 菜单少一项，不是崩溃
- `module.yaml` 非法的模块拒收并说明原因——校验，而不是挑选

## 后置工作

- **工具执行器**：按 `tools` 清单真正拉起模块自带的可执行物，含权限分级与沙箱
- **工具通道托管**：登记处同时管理工具调用的外部服务凭据（模块只持 id）
- **`model.config.yaml` 写入**：目前只读；写入路径待接入（运行期改选供应商即落盘）
- **`prefer` 选模型**：module.yaml 已预留字段，多模型供应商下按用途选模型
- 编排形态的进一步刻度：部分聚合（几个模块合成一个智能体）+ 其余分权协商的混合拓扑
- 执行期并行调度（无依赖任务同时跑）与讨论上下文压缩
- 模块分发：打包、签名、市场
- 供应商管理界面：密钥轮换、用量统计
