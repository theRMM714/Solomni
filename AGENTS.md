# 项目规则

## 核心约束
- 项目始终为GREEN FIELD，没有任何历史包袱
- 项目不存在版本更替，进行任何修改也始终为唯一版本，不要擅自确定版本号等。
- 本仓库是唯一存在的开发仓库，永远不用考虑其它开发者的兼容性，不要因考虑其它地方的旧版本兼容性而生产废代码
- 贯彻项目理念永远高于兼容性
- 有问题和困惑不要隐瞒或独自推理，直接询问用户
- 用户要求和本规则冲突时警告用户获得肯定答复后再执行
- 主AI主要做分析和决策，简单工作自行实现，token消耗大的复杂工作尽量使用Subagent进行

## 代码规范
- 必须每个模块可进行单独mock测试
- 整体必须遵循DIP（依赖倒置）原则
- 注释仅描述当前代码功能和联动模块

## 分层纪律（依赖倒置的落点）
- 依赖箭头只允许：presentation → core ← adapters，装配（main 组合根）→ 全部
- **core/**：定义抽象（ports）+ 编排业务。禁用 std::fs 读文件、ureq、stdin/stdout——机制一律下沉适配层
- **adapters/**：实现 core 的端口。只依赖 core，永不反向；可引用外部库（ureq/serde_yaml）
- **presentation/**：渲染事件、收集输入。只依赖 core 门面，永不接触端口对象
- **main.rs**：组合根，唯一允许 new 具体适配器的地方（依赖注入）；除装配外无业务
- 「用哪个供应商」等选择策略在 core（解析链）；「怎么建通道」等机制在 adapters；两者不互换
- 端口只画在 IO 与可替换点上：纯逻辑（信封解析、引擎）刻意不抽象，不做万物的 trait

## 运行日志（Log 端口）
- core 定义 Log 端口（ports.rs）：info/warn/error 三级；关键节点（通道降级、HTTP 失败、会话动作失败、装配失败）必须埋点
- 适配层实现（adapters/log.rs）：每次运行在根目录 logs/ 下按时间戳建一个 .log 文件；logs/ 不入库
- 组合根创建唯一 FileLog 注入 Core 与呈现层；测试用 NoopLog

## 提示词册（prompts.yaml）
- 所有发给 LLM 的提示词一律写入 prompts.yaml，禁止硬编码进代码；改文案只改册子
- 占位符用 {{key}}；渲染器在 core/prompt.rs（纯逻辑），文件加载经 PromptSource 端口在适配层
- 缺文件/缺键/缺变量 = 报错暴露，禁止静默兜底文案
- 界面通知（[建组]/[上限] 等）是呈现层文案，不属于提示词册

## 路径规范（保证项目跨平台/跨目录可迁移）
- 代码与文档中的项目内路径一律使用**相对路径**，以产品根（Cargo.toml 所在目录）为基准，禁止出现 `/mnt/…`、`/home/…`、`C:\…` 等机器相关绝对路径
- 代码拼接路径用路径组件（如 `root.join(".home").join("providers.yaml")`），禁止把 `/` 或 `\\` 写进字符串再拼（分隔符交给运行环境）
- 引用仓库内文件（文档、源码、模块清单）同样用相对形式：`README.md`、`modules/<id>/module.yaml`
- 例外（不算绝对路径违规）：①`.gitignore` 的行首 `/` 与 `**/` 是 git 语义；②URL 拼接（如 `{base_url}/chat/completions`）遵循 URL 规则，不是文件路径；③用户私有区（如 `.home/`、模块 `userdata/`）整体搬迁仍以产品根为锚，仅通过环境变量改写根，不引入机器路径

## BUG修复规则
- 低级bug，代码写错了，可以直接修改。
- bug的原因是架构有问题，设计有问题等非低级错误，必须将情况告诉用户，由用户决定如何修改。

## 关键文档阅读指导
- 项目概括：README.md
- 产品与运行流程：PRODUCT.md
- 项目理念：PHILOSOPHY.md
- 模块契约（开发模块必读）：MODULE_SPEC.md