# AGENTS.md

> 本文件是常驻硬约束。架构、分层、端口、日志、提示词册等展开规则见 [ARCHITECTURE.md](ARCHITECTURE.md)；但核心项目规定、路径、代码、执行分流、Git 规则必须常驻本文件，避免 agent 未读取其它文档时失去约束。

## 效力与优先级

- 本文件必须始终生效。
- 若本文件与其它项目文档冲突，以本文件为准，并提示用户。
- 用户要求与本文件冲突时，先警告用户；获得肯定答复后再执行。
- 有问题和困惑不要隐瞒，不要独自推理，直接询问用户。
- 一切文档仅描述当前状态，不对已经迭代的版本进行任何描述。

## 核心约束：GREEN FIELD

- 项目始终为 GREEN FIELD，没有任何历史包袱。
- 本项目为跨平台项目，进行任何实现必须优先考虑跨平台兼容性，默认覆盖 Windows/macOS/Linux
- 项目不存在版本更替。进行任何修改也始终为唯一版本，不要擅自确定版本号等。
- 本仓库是唯一存在的开发仓库，永远不用考虑其它开发者的兼容性。
- 不要为旧版本兼容性、已经不存在的旧实现生产废代码或无意义文档描述。
- 贯彻项目理念永远高于兼容性。

## 常驻硬约束

### 路径

- 代码与文档中的项目内路径一律使用相对路径，以产品根（Cargo.toml 所在目录）为基准。
- 禁止出现 `/mnt/…`、`/home/…`、`C:\…` 等机器相关绝对路径。
- 拼接路径必须用路径组件，例如 `root.join(".home").join("providers.yaml")`。
- 禁止把 `/` 或 `\` 写进字符串再拼路径，分隔符交给运行环境。
- 默认不使用本项目外的目录安装依赖或存放配置。必须先尝试把一切环境收敛到本项目目录中；若确需使用项目外目录，先向用户说明并获同意。
- 引用仓库内文件同样使用相对形式，例如 `README.md`、`modules/<id>/module.yaml`。

例外，不算绝对路径违规：

1. `.gitignore` 的行首 `/` 与 `**/` 是 git 语义。
2. URL 拼接，例如 `{base_url}/chat/completions`，遵循 URL 规则，不是文件路径。
3. 用户私有区，例如 `.home/`、模块 `userdata/`，整体搬迁仍以产品根为锚，仅通过环境变量改写根，不引入机器路径。

详细说明见 [ARCHITECTURE.md](ARCHITECTURE.md)，但以上硬规则始终生效。

### 代码

- 整体必须遵循 DIP（依赖倒置）原则。
- 保持高内聚低耦合，按业务功能进行垂直切分。
- 业务之间用 api 接口调用，不直接互相引用。
- 必须任意环节、任意实现都必须通过测试，详见 [TESTING.md](TESTING.md)。
- 项目为跨平台项目，任何实现先要想能否支持跨平台。
- 注释仅描述当前代码功能和联动模块。
- 不生产无意义文档描述。

### Git

- 完成代码和本地测试后进行本地 git 提交。
- 任何提交前检查 `.gitignore` 是否正确配置，使用 `git status` 检查是否有意外被跟踪的文件。
- git 推送只在需要真机测试或 actions 测试时进行。
- 其它情况执行 git 推送前必须获得用户明确授权。

## 推理策略

- 推断前先判断能否用工具直接取得确切证据；能，就直接取得证据，不凭猜测推理。
- 若没有确切证据，检查抛出问题的代码或日志是否不够清晰；若不够清晰，应优先优化代码或日志以暴露证据。
- 若需要用户提供资料，先判断能否让用户更方便地提供；能简化就让用户提供更易获取的资料。
- 证据不足且无法通过工具取得时，直接询问用户。

## 执行分流

处理任何请求前，先按以下决策树分类。分类不明确时，按更高风险类别处理。

### 决策树

收到请求
├─ 是否属于“任务”？
│  ├─ 是 → 任务流程
│  │  ├─ 先总结整体实施方案
│  │  ├─ 等用户审查并同意执行
│  │  └─ 同意后开始执行
│  └─ 否 → 继续
├─ 是否是 bug？
│  ├─ 否 → 直接询问用户，不得自行扩大范围
│  └─ 是 → 继续
├─ 是否只属于低级 bug？
│  ├─ 是 → 低级 bug 流程
│  │  └─ 直接修；不必先总结方案，不必等待审批
│  └─ 否 → 非低级 bug 流程
│     ├─ 先说明现象、证据、影响范围、可能根因、候选方案
│     ├─ 由用户决定如何修改
│     └─ 不得擅自修改
└─ 是否不确定？
   └─ 按非低级 bug 处理，先询问用户

### 判定优先级

1. 先判断是否属于“任务”。
2. 若不属于任务，再判断是否属于 bug。
3. 若属于 bug，再判断是否只属于低级 bug。
4. 不确定时，按非低级 bug 处理。
5. 混合请求按最高风险类别处理。

### 混合请求

- 同时包含任务、低级 bug、非低级 bug 时，按最高风险类别处理。
- 若任务中包含低级 bug，可随任务一起处理，但仍需先走任务审批。
- 若非低级 bug 的修复会改变接口、架构、数据模型、依赖关系或目录结构，按任务处理，先给方案并等待审批。

### 任务

“任务”指会改变项目行为、契约、结构或设计的改动，包括但不限于：

- 新功能
- 重构
- 接口/契约变更
- 跨模块改动
- 行为语义变更
- 架构调整
- 依赖关系变更
- 目录结构变更

任务执行前必须先总结整体实施方案，等用户审查并同意执行后，才开始执行。

### 低级 bug

低级 bug 指：

- 拼写错误
- 变量写错
- 局部逻辑笔误
- 明显边界条件写错
- 且不改变接口、架构、行为设计、数据模型、依赖关系、目录结构

低级 bug 直接修，不必先总结方案，不必等待审批。

### 非低级 bug

非低级 bug 指由以下原因导致的 bug：

- 架构
- 设计
- 接口
- 数据模型
- 并发
- 安全
- 依赖关系

非低级 bug 必须先说明情况，由用户决定如何修改，不得擅自修改。

### 不确定时

不确定是不是低级 bug 时，按非低级 bug 处理，先询问用户。

## 复杂任务与 subagent

- 复杂任务请灵活运用 subagent 功能。
- 若当前工具支持 subagent，优先拆分并行验证。
- 若当前工具不支持 subagent，则按阶段串行执行，不幻想不存在的功能。

## 文档指路

- 项目概括与快速开始：[README.md](README.md)
- 产品与运行流程：[PRODUCT.md](PRODUCT.md)
- 项目理念：[PHILOSOPHY.md](PHILOSOPHY.md)
- 架构与开发规则：[ARCHITECTURE.md](ARCHITECTURE.md)（改代码前必读：分层、端口、模块地图、日志、提示词册、落盘契约）
- 模块契约：[MODULE_SPEC.md](MODULE_SPEC.md)（开发模块必读：打包格式、发言信封、回报与验收、工具与路径模型、测试交付）
- 运行包契约：[RUNTIME_SPEC.md](RUNTIME_SPEC.md)（做运行包必读：package.yaml、两种 kind、校验与拒收）
- 登记处契约：[REGISTRY_SPEC.md](REGISTRY_SPEC.md)（providers / models / agents / settings 四份 yaml 的字段与安全边界）
- 测试架构与缺口账：[TESTING.md](TESTING.md)（测试唯一权威：T0-T5、替身语义、端口矩阵、质量门禁、缺口账）